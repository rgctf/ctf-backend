[package]
name = "backend-models"
version = "0.1.0"
authors = []
edition = "2018"

[features]
default = ["serde"]

[dependencies]
chrono = "0.4.19"
serde = { version = "1.0", features = ["derive"], optional = true }
